package ch.capi.net{	import flash.errors.IllegalOperationError;			import ch.capi.data.Set;			/**	 * Utility class to register the named <code>ICompositeMassLoader</code> instances. 	 * 	 * @see			ch.capi.net.ICompositeMassLoader ICompositeMassLoader	 * @author		Cedric Tabin - thecaptain	 * @version		1.0	 */	public class CompositeMassLoaderRegisterer	{		//---------//		//Constants//		//---------//				//---------//		//Variables//		//---------//		private static var __compositeMassLoaders:Set	= new Set();				//--------------//		//Public methods//		//--------------//		/**		 * Enumerates all the <code>ICompositeMassLoader</code> registered.		 * 		 * @return	An <code>Array</code> containing the names of the registered <code>ICompositeMassLoader</code>.		 */		public static function enumerateAll():Array		{			return __compositeMassLoaders.keys();		}		/**		 * Retrieves the specified <code>ICompositeMassLoader</code>.		 * 		 * @param	cmlName	The name of the <code>ICompositeMassLoader</code>.		 * @return	The <code>ICompositeMassLoader</code>.		 * @throws	ch.capi.errors.DataError	If the <code>ICompositeMassLoader</code> doesn't exist.			 */		public static function get(cmlName:String):ICompositeMassLoader		{			return __compositeMassLoaders.getValue(cmlName) as ICompositeMassLoader;		}		/**		 * Registers the specified <code>ICompositeMassLoader</code>. It must be named otherwise		 * a <code>IllegalOperationError</code> will be thrown.		 * 		 * @param	cml		The <code>ICompositeMassLoader</code>.		 * @throws	IllegalOperationError	If the <code>ICompositeMassLoader</code> is not named.		 * @throws	ch.capi.errors.DataError	If an <code>ICompositeMassLoader</code> with the same name		 * 						already exists.		 */		public static function register(cml:ICompositeMassLoader):void		{			if (cml.name == null) throw new IllegalOperationError("The CompositeMassLoader is not named");			__compositeMassLoaders.put(cml.name, cml);		}		//-----------------//		//Protected methods//		//-----------------//				//---------------//		//Private methods//		//---------------//	}}